name: Upload Python Package to PyPI when a Release is Created

on:
  release:
    types: [created]

jobs:
  pypi-publish:
    name: Publish release to PyPI
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/msad-query
    permissions:
      id-token: write
    defaults:
      run:
        working-directory: ./
    steps:
      - uses: actions/checkout@v4
      - name: Install Asciidoctor
        run: sudo apt-get install asciidoctor
      - name: Convert asciidoc(s) to rst(s)
        run: |
          asciidoctor changes.asciidoc -o CHANGES.rst
          asciidoctor license.asciidoc -o LICENSE.rst
          asciidoctor readme.asciidoc -o README.rst
      - name: Replace asciidoc references with rst
        run: |
          sed -i -e "s/readme.asciidoc/README.rst/g" setup.py
          sed -i -e "s/license.asciidoc/LICENSE.rst/g" setup.py
          sed -i -e "s/.asciidoc/.rst/g" manifest.in
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.14.x"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build
      - name: Remove build cache
        run: rm -rf build dist *.egg-info
      - name: Build package
        run: |
          ls -lha
          python -m build
      - name: Publish package distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1